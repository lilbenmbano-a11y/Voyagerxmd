services:
  - type: web
    name: scotty-c
    runtime: node
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: PORT
        value: 3100
      - key: BOT_NAME
        value: Spider 🕷️ voyage
      - key: NODE_ENV
        value: production
    autoDeploy: true
